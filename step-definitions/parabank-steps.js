module.exports = function () {


/*

  this.Given(/^I am on the "([^"]*)" website$/, function (objectKey) {
    return helpers.loadPage(page.parabank[objectKey],2000);

  });

  this.When(/^I click on "([^"]*)"$/, function (objectKey) {
    return page.parabank.clickElement(objectKey)

  });

  this.When(/^I enter inputs for registering$/, async function (table) {
    const fields = table.rows();
    for (var i = 0; i < fields.length; i++) {
      page.parabank.inputElement(fields[i][0], fields[i][1]);
    }
    await driver.sleep(3000);
    return;
  });


  /* this.When(/^I logged into parabank website as "([^"]*)" and "([^"]*)"$/, function(username, password) {
    
    page.parabank.inputElement('username', 'username');
    page.parabank.inputElement('password', 'password');
    return;
   
  });
   

  this.When(/^I enter "([^"]*)" as "([^"]*)"$/, function (inputname, value) { 
    return page.parabank.inputElement(inputname, value);
  });
  
  this.Then(/^I should see "([^"]*)"$/, function (objectKey) {
    console.log("The objectKey in commonsteps is: " + objectKey);
    return page.parabank.elementExists(objectKey);
  });
   
  this.When(/^I am logged into the parabank website as "([^"]*)" and "([^"]*)"$/, function(Name, Pass) {
    helpers.loadPage(page.parabank.parabank);
    page.parabank.inputElement('Name', Name);
    page.parabank.inputElement('Pass', Pass);
    page.parabank.clickElement('Login');
   // page.parabank.elementExists('CustomerPage');
    return
});

*/

}

